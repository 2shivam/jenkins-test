pipeline {
    agent any
    stages {
        stage("build") {
            steps {
                echo "building a maven project"
            }
        }
        stage("package") {
            steps {
                echo "package a maven project"
            }
        }
        
    }
}
